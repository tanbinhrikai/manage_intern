spring:
  application:
    name: backend
  datasource:
    url: ${URL_DATABASE}
    username: root
    password: dongle170503
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
jwt:
  signerKey: ${SIGNER_KEY}