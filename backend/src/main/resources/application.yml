spring:
  application:
    name: backend
  datasource:
    url: ${URL_DATABASE}
    username: myuser
    password: Password@123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
jwt:
  signerKey: ${SIGNER_KEY}