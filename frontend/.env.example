VITE_API_URL=  // Set the base URL for API requests in the frontend application. 
// This should point to the backend server's address and port. 
// For example, if your backend is running on localhost at port 8080, you would set it as follows:
// VITE_API_URL="http://localhost:8080"